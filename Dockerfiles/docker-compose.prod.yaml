name: calendar-app

services:

  database:
    image: mongo
    restart: unless-stopped
    env_file: ../.env
    networks:
      - calendar-network
    volumes:
      - ../data:/data/db


  server:
    restart: unless-stopped
    env_file: ../.env
    image: brandonmartindev/calendar-server:1.0.0
    networks:
      - calendar-network
    ports:
      - 8080:8080
    depends_on:
      - database

    
  client:
    restart: unless-stopped
    image: brandonmartindev/calendar-client:1.0.0
    networks:
      - calendar-network
    ports: 
      - 80:80
    depends_on:
      - server


networks:
  calendar-network:
    driver: bridge